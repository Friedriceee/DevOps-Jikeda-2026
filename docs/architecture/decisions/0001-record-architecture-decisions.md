# 1. 用 ADR 记录架构决策

- 状态：已接受
- 日期：2026-01

## 背景

架构决策需要可追溯，方便写报告和回顾。

## 决策

用 Architecture Decision Record（ADR）记录每个重要决策，一个决策一个文件，编号递增，放在 `docs/architecture/decisions/`。

格式：背景 / 决策 / 影响。状态取值：提议、已接受、已废弃、被取代。

## 影响

- 每次做出影响结构的选择时新增一条 ADR。
- 报告的「关键架构决策」章节直接引用这些 ADR。
